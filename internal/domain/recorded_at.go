package domain

import "time"

// RecordedAt — момент, когда строка появилась в книге, если его вообще можно
// узнать.
//
// ЗАГЛУШКА: тело пишется следующим коммитом, здесь оно нужно только чтобы
// красный тест был красным по существу, а не ошибкой сборки.
func RecordedAt(_ string) (time.Time, bool) {
	return time.Time{}, false
}
