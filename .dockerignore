# Keep the build context small and reproducible. frontend/dist is deliberately
# excluded: the web stage builds the bundle inside the image, so whatever a
# developer happens to have lying around locally cannot leak into an image.
.git
.github
bin/
*.out
coverage.out
gitleaks*
frontend/node_modules
frontend/dist
*.md
.gitleaks.toml
lefthook.yml
