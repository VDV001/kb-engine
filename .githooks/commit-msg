#!/usr/bin/env bash
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
if command -v lefthook >/dev/null 2>&1; then
  exec lefthook run commit-msg "$1"
fi
exec bash scripts/commit-msg-lint.sh "$1"
