# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist is intentionally committed: web.go go:embed's frontend/dist so the
# kbengine binary builds standalone (a fresh `git clone` + `go build` has no
# Node toolchain to regenerate it). Rebuild with `just web` after UI changes.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
